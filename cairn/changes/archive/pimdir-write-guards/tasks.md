---
cairn: tasks
change: pimdir-write-guards
---

# Tasks

- [x] `update_addressbook` refuses a rename, explaining that the display name is the sync's
- [x] A collection guard fails `card list` and `card create` on an unknown addressbook
- [x] The listing previews from the summary when a body is missing, and warns
- [x] cargo fmt, clippy, tests, feature matrix
- [x] Re-test against both a bare and a seeded store
- [x] Update the report, fold the delta, log and archive
