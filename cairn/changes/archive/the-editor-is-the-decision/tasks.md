---
cairn: tasks
change: the-editor-is-the-decision
---

# Tasks

- [x] Drop the menu from the composer: `review`, `CardChoice` and the preview branch.
- [x] Read an untouched card back as an abandoned edit, alongside the emptied file.
- [x] Add `-i/--composer` to `card build`, resolving the account only when `-i` carries no `--composer`.
- [x] Add `-o/--output` to `card build`, answering a `Message` when it writes a file.
- [x] Print nothing when an interactive build is abandoned.
- [x] Refuse a blank source, and a multi-card source under a field flag.
- [x] Document it in CHANGELOG.md and config.sample.toml.
- [x] Fold the delta into the spec and write the log entry.
