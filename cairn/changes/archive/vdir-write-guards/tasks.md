---
cairn: tasks
change: vdir-write-guards
---

# Tasks

- [x] io-vdir: the collection update removes the metadata file of an absent or empty value
- [x] io-vdir: tests for setting, clearing and leaving metadata alone
- [x] cardamum: `addressbook_path` resolves the collection and fails naming it
- [x] cardamum: `update_card` reads the item first, and bails on `--if-match`
- [x] cargo fmt, clippy, tests, feature matrix
- [x] Re-test against a throwaway vdir instance, both surfaces
- [x] Update the report, fold the delta, log and archive
